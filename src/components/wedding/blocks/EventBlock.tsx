import Section from "../ui/Section";
import Countdown from "../ui/Countdown";
import GlassCard from "../ui/GlassCard";
export default function EventBlock({ background, eyebrow, title, date, countdown, events, animation }: any) { return <Section id="event" background={background} animation={animation}><div className="mx-auto max-w-6xl"><div className="text-center"><p className="text-xs tracking-[.3em] uppercase opacity-60">{eyebrow}</p><h2 className="font-heading mt-3 text-6xl md:text-8xl">{title}</h2><p className="mt-4 text-sm tracking-[.15em] opacity-70">{date}</p></div>{countdown?.enabled && <div className="mx-auto mt-10 max-w-2xl"><Countdown target={countdown.target}/></div>}<div className="mt-14 grid gap-5 md:grid-cols-2">{events?.map((event: any, i: number) => <GlassCard key={i} className="p-7 md:p-9"><p className="text-xs tracking-[.25em] uppercase opacity-55">{event.type ?? "WEDDING EVENT"}</p><h3 className="font-heading mt-3 text-4xl">{event.title}</h3><p className="mt-5 text-sm">{event.time}</p><p className="mt-2 text-sm opacity-70">{event.venue}</p><p className="mt-1 text-xs opacity-55">{event.address}</p>{event.mapUrl && <a href={event.mapUrl} target="_blank" rel="noreferrer" className="mt-7 inline-block rounded-full border border-white/20 px-5 py-3 text-xs tracking-[.15em]">{event.button ?? "VIEW LOCATION"}</a>}</GlassCard>)}</div></div></Section>; }

"use client";
import Section from "../ui/Section";
import { Camera as Instagram } from "lucide-react";
export default function HomeBlock({ background, eyebrow, title, personalized, social, animation }: any) { return <Section id="home" background={background} animation={animation} className="flex items-end pb-24 md:pb-28"><div className="mx-auto w-full max-w-6xl"><div className="max-w-xl"><div className="mb-8 h-px w-32 bg-white/50"/><p className="text-xs tracking-[.3em] uppercase opacity-70">{eyebrow}</p><h2 className="font-heading mt-3 text-6xl md:text-8xl">{title}</h2><div className="glass mt-10 max-w-md rounded-[26px] p-5"><p className="text-sm opacity-60">{personalized?.label}</p><p className="font-heading mt-1 text-3xl">{personalized?.guestName}</p><p className="mt-2 text-sm opacity-75 whitespace-pre-line">{personalized?.parents}</p></div>{social?.username && <a href={social.url ?? `https://instagram.com/${social.username.replace(/^@/, "")}`} target="_blank" rel="noreferrer" className="glass mt-5 inline-flex items-center gap-2 rounded-full px-5 py-3 text-xs tracking-[.15em]"><Instagram size={16}/>{social.username}</a>}</div></div></Section>; }

"use client";
import { useState } from "react";
import Section from "../ui/Section";
import { motion } from "framer-motion";
export default function SouvenirBlock({ background, eyebrow, title, description, card, interaction, animation }: any) { const [rotate,setRotate]=useState({x:0,y:0}); const tilt=interaction?.tilt!==false; return <Section id="souvenir" background={background} animation={animation}><div className="mx-auto max-w-5xl text-center"><p className="text-xs tracking-[.3em] uppercase opacity-60">{eyebrow}</p><h2 className="font-heading mt-3 text-6xl md:text-8xl">{title}</h2><p className="mx-auto mt-5 max-w-xl text-sm opacity-65">{description}</p><div className="mt-12 flex justify-center" style={{perspective:1000}} onMouseMove={(e)=>{if(!tilt)return;const r=e.currentTarget.getBoundingClientRect();setRotate({x:((e.clientY-r.top)/r.height-.5)*-10,y:((e.clientX-r.left)/r.width-.5)*10})}} onMouseLeave={()=>setRotate({x:0,y:0})}><motion.div animate={{rotateX:rotate.x,rotateY:rotate.y}} transition={{type:"spring",stiffness:180,damping:18}} className="relative w-full max-w-sm overflow-hidden rounded-[30px] border border-white/25 bg-black/30 p-5 shadow-2xl backdrop-blur-xl"><img src={card?.image} alt="Souvenir" className="aspect-[4/5] w-full rounded-[22px] object-cover"/><div className="p-5"><p className="text-[10px] tracking-[.25em] opacity-55">SPECIAL GUEST</p><h3 className="font-heading mt-2 text-4xl">{card?.guestName}</h3><p className="mt-2 text-sm opacity-65">{card?.couple}</p><p className="mt-4 text-xs tracking-[.15em] opacity-55">{card?.date} · {card?.location}</p>{card?.number&&<p className="mt-3 text-xs opacity-45">#{card.number}</p>}</div></motion.div></div></div></Section>; }

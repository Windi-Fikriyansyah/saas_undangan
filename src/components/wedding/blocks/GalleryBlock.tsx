"use client";
import { useState } from "react";
import Section from "../ui/Section";
import Lightbox from "../ui/Lightbox";
export default function GalleryBlock({ background, eyebrow, title, description, images = [], layout = "masonry", lightbox = true, animation }: any) { const [index,setIndex]=useState<number|null>(null); const cls = layout === "grid" ? "grid grid-cols-2 md:grid-cols-3" : layout === "single" ? "grid grid-cols-1" : "columns-2 md:columns-3"; return <><Section id="gallery" background={background} animation={animation}><div className="mx-auto max-w-7xl"><div className="mb-12 max-w-xl"><p className="text-xs tracking-[.3em] uppercase opacity-60">{eyebrow}</p><h2 className="font-heading mt-3 text-6xl md:text-8xl">{title}</h2><p className="mt-4 text-sm opacity-65">{description}</p></div><div className={`${cls} gap-3 md:gap-5`}>{images.map((img:any,i:number)=><button key={i} onClick={()=>lightbox&&setIndex(i)} className={`group mb-3 block w-full overflow-hidden rounded-[22px] text-left md:mb-5 ${layout === "masonry" ? "break-inside-avoid" : ""}`}><img src={img.src} alt={img.alt ?? `Gallery ${i+1}`} loading="lazy" className="w-full object-cover transition duration-1000 group-hover:scale-[1.04]"/></button>)}</div></div></Section><Lightbox images={images} index={index} onClose={()=>setIndex(null)} onChange={setIndex}/></>; }

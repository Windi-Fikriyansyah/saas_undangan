import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const t = await prisma.template.findFirst({ where: { name: { contains: 'minimalist-1', mode: 'insensitive' } } }); if (t) { console.log('Keys:', Object.keys(t.configJson as any)); const config = t.configJson as any; if (Array.isArray(config)) { console.log('Is Array', config.length); } else { console.log('Is Object', config.content ? 'has content' : 'no content array'); } } else { console.log('Not found'); } } main().finally(() => prisma.());